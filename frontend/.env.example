# LOCAL VOICE Frontend Environment Variables

# The base URL of the backend API.
# Leave it blank in development to let Vite proxy requests to http://localhost:8080,
# or set it if the backend is deployed elsewhere.
VITE_API_URL=http://localhost:8080
